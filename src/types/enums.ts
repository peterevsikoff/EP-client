enum PAGES {
    SIGN_IN = "signIn",
    SIGN_UP = "signUp",
    HOME = "home",
    TESTS = "tests",
    ABOUT = "about",
    ADMIN = "admin",
    USERS = "users",
}

enum INPUTTYPE {
    EMAIL = "email",
    PASSWORD = "password",
    TEXT = "text",
}

export {
    INPUTTYPE,
    PAGES,
}